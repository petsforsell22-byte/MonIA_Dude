name: Build DUDE APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
      - name: Build with Buildozer
        uses: Niklas-S/buildozer-action@v1
        with:
          command: buildozer android debug

